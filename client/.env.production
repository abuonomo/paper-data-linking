# This file is committed to git. Do not add secrets here.
# Set your real production values in .env.production.local (gitignored),
# which Vite loads with higher priority than this file at build time.
VITE_BASE_URL=paper-data.example.com
VITE_BASE_PROTOCOL=https
VITE_CONTACT_EMAIL=you@example.com